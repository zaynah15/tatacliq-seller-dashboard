# Optional — without this, enrichment uses a deterministic fallback that
# still produces a valid enriched .xlsx, so the demo works out of the box.
#
# To get a real Gemini-powered enrichment:
#   1. Go to https://aistudio.google.com (sign in as zaynah15mahmood@gmail.com)
#   2. Get API key -> Create API key
#   3. Paste below, then restart `npm run dev`
GEMINI_API_KEY=
